## Configuração do Ambiente do Cliente (Máquina na Hml) ##

server:
  port: 8082

# Config do service discovery
eureka:
  host: localhost
  name: eureka
  port: 8081

## Config Database
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/vtswebapi?useSSL=false&serverTimezone=UTC&useLegacyDatetimeCode=false
    username: root
    password: vs368797
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
    show-sql: true
    hibernate:
      ddl-auto: update